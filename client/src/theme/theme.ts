"use client";
import { createTheme } from "@mui/material";
export const customTheme = createTheme({
  typography: {
    fontFamily: "var(--font-inter)",
  },
  palette:{
    text: {
      primary: '#3C435C',
    },
    },
});
